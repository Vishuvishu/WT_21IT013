arr = [1,2,3,45,6,7,8,9,10];
//FOREAC  //MAP //SOEM //FILER //FIND EVERY //VGSWGH

function foreach_func(){
    arr.forEach(function(arr){
        //document.getElementById("HQLLO").appendChild=arr;
        let X = document.createElement("P")
        X.innerText=arr;
        document.body.appendChild(X)
    })
}
